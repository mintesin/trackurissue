//to mail the employee on registration and recovery.
//accepts email, name and message
//it must do work with promise with resolve and reject 

function userMailer(userEmail,userFullName, message){
    return new Promise(resolve,reject)
}

export deafult userMailer